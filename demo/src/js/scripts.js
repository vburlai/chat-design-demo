function init() {
    setupSVGRefresh();
}

function setupSVGRefresh() {
    // const el = document.getElementsByClassName('svg')[0];
    // if (el) {
    //     el.addEventListener('load', () => {
    //         setTimeout(() => el.contentWindow.location.reload(), 5000);
    //     })
    // }
}

init();
